import Product from './Product';
// import React from 'react';
const ProductList = () => {
    const productList= [1,2,3,4,5];

// create the list of objects.. mock an API response basically

//mpa the objects into separate instances of the Product component
}
export default ProductList;